Skipily Android Release Keystore
=================================
Datei:      skipily-release.keystore
Alias:      skipily
Passwort:   -> steht in android/keystore.properties (gitignored)
Gültigkeit: 10.000 Tage (~27 Jahre)

WICHTIG: Keystore-Datei UND keystore.properties SICHER aufbewahren und
niemals ins Git-Repository! Ohne den Keystore (inkl. Passwort) können
keine Updates im Play Store veröffentlicht werden.

Empfehlung: Keystore + Passwort in 1Password / sicheren Cloud-Ordner ablegen.

Hinweis: Das Passwort war früher hier und in app/build.gradle im Klartext
und ist daher noch in der Git-Historie. Da der Keystore selbst nie im Git
lag, ist das unkritisch — bei Bedarf Passwort via `keytool -storepasswd`
und `keytool -keypasswd` ändern und keystore.properties anpassen.
